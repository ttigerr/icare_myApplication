<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.blueradix.andriod.icare_myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Icare_myApplication">
        <activity android:name=".activityScreen.AddRecordScreen"></activity>
        <activity android:name=".activityScreen.RecordContentScreen" />
        <activity android:name=".activityScreen.SymptomContentScreen" />
        <activity android:name=".activityScreen.RecordHealthScreen" />
        <activity
            android:name=".activityScreen.DoctorContentScreen"
            android:label="@string/title_activity_doctor_content_screen"
            android:theme="@style/Theme.Icare_myApplication.NoActionBar" />
        <activity android:name=".activityScreen.DoctorScreen" />
        <activity android:name=".activityScreen.SymptomScreen" />
        <activity android:name=".activityScreen.RegisterScreen" />
        <activity android:name=".activityScreen.LoginScreen" />
        <activity android:name=".activityScreen.HomeScreen" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>